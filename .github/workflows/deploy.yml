name: Deploy KingKong Climbing to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

# 不取消进行中的部署：Pages 部署被中途取消会报 "Deployment failed, try again later"。
# 对齐 GitHub 官方 Pages 工作流，连续推送时排队而非互相取消。
concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: package-lock.json
      - run: npm ci
      - run: npm test
      - run: npm run build
      # enablement: 历史重写强推后 GitHub 曾把 Pages 站点下线（configure-pages 404），
      # 站点不存在时由工作流自动重新启用（需要上方 pages: write 权限）。
      - uses: actions/configure-pages@v6
        with:
          enablement: true
      - uses: actions/upload-pages-artifact@v5
        with:
          path: packages/platform-web/dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v5
