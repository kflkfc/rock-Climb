{
  "name": "boulder-sim",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "硬核抱石模拟器 — 核心垂直切片 Web Demo (V4 接触式拖拽 + 耐力匹配)",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.10",
    "vitest": "^2.1.4"
  }
}
